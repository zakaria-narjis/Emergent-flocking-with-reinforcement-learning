# fast_debug.yaml — quick smoke-test overrides (~2 min run)
# Usage: python train.py --override-config config/experiments/fast_debug.yaml --run-name debug

training:
  total_steps: 5000
  eval_interval: 1000
  eval_n_episodes: 5

simulation:
  steps: 50
  population: 3
